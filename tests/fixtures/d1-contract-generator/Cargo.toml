[package]
name = "d1-contract-generator"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
mech-core = { path = "../../../src/core", default-features = false, features = ["resident-execution", "std"] }
mech-engine = { path = "../../../src/engine", default-features = false, features = ["resident-artifact", "compiler_default"] }
serde_json = "1.0.149"
sha2 = "0.10"

[patch.crates-io]
mech-bytecode = { path = "../../../src/bytecode" }
mech-core = { path = "../../../src/core" }
mech-engine = { path = "../../../src/engine" }
mech-syntax = { path = "../../../src/syntax" }

[workspace]
