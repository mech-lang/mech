@input := test-resource://input/value{:read(sample)}
sample := @input/sample

~state := 0.0
state += sample

@transactional := test-resource://transactional/state
@transactional/value <- state

output := state
