# Mech Runtime

`mech-runtime` provides the configured runtime, host integration, capability
enforcement, transactional resource effects, and native-application execution
support for Mech programs.

It is published as a component crate and is used by generated native hosted
applications with only the value, host, and runtime features selected by their
native build plan.
