config := {
  hosts: [
    {
      name: "clock"
      provider: "time"
      settings: {
        interval-ms: 1000
      }
    }
    {
      name: "console"
      provider: "console"
      settings: {}
    }
  ]

  run: {
    paths: ["clock.mec"]

    grants: [
      {
        target: "clock/clock"
        operations: ["read"]
        paths: ["unix-ms", "hour", "minute", "second", "millisecond"]
      }
      {
        target: "console/output"
        operations: ["write"]
        paths: ["line"]
      }
    ]
  }
}
