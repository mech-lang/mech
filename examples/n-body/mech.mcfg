config := {
  runtime: {
    name: "n-body"

    resident-durability: "volatile"

    limits: {
      max-turn-duration-ms: 2000
    }
  }

  serve: {
    address: "127.0.0.1"
    port: 8081
    paths: ["."]
    presentation: "output"
  }

  hosts: [
    {
      name: "clock"
      provider: "timer"
      settings: {
        frequency-hz: 60
        max-catch-up-steps: 1
        queue-policy: "latest"
      }
    }

    {
      name: "orbit"
      provider: "scene"
      settings: {
        renderer: "output"
      }
    }
  ]

  run: {
    paths: ["n-body.mec"]

    grants: [
      {
        target: "clock/tick"
        operations: ["read"]
        paths: ["tick"]
      }

      {
        target: "orbit/frame"
        operations: ["write"]
        paths: ["replace"]
      }
    ]
  }
}
