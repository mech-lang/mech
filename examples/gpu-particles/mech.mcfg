config := {
  runtime: {
    name: "gpu-particles"
    limits: {
      max-turn-duration-ms: 10000
    }
  }

  run: {
    paths: ["particles.mec"]
    executor: {
      provider: "gpu"
      turns: 120
    }
  }
}
