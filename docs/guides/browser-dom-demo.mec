Browser DOM Demo
===============================================================================

The browser DOM demo uses a manifest-backed context import.

```mech:disabled
+> @browser := browser/dom
```

See `examples/browser-dom-demo` and `hosts/browser/docs` for the host config flow.
