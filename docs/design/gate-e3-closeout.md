# Gate E3 closeout

E3 retires the machinery that controlled the D3/D4 migration. It does not
change the resident runtime, retained hosts, compiler semantics, bytecode-v1
format, or benchmark evidence produced by E2.

## Stack identity

- Branch: `refactor/e3-retire-migration-scaffolding`
- Base branch: `refactor/e2-consolidate-resident-architecture`
- Exact E2 base: `f16e6b5d8a01b226353aba914c7c8ac1fcc90e31`
- E3 implementation head before this closeout commit:
  `b46391153eea4c345da6f4a972c0f4081b80bae7`
  (`test(value-system): reconcile post-migration inventory`)
- Final E3 head: the commit containing this document. Its exact external SHA
  is recorded in the draft PR description and the full-validation run because
  a commit cannot contain its own content-addressed identity.

E1, E2, and E3 remain stacked, draft, and unmerged during review.

## Ordered E3 commits

1. `refactor(ci): promote permanent architecture qualification`
2. `test(architecture): retire D3 and D4 migration projections`
3. `refactor(build,bytecode): remove experimental actor migration fixtures`
4. `test(value-system): reconcile post-migration inventory`
5. `docs(architecture): close Gate E migration`

For Commit 4, the inventory generator found `current-inventory.json` current.
The permanent D0 activation projection was mechanically reconciled by removing
six deleted legacy occurrences and re-anchoring surviving line coordinates.
The exhaustive contract then produced exactly the one controlled stale-evidence
finding assigned to F0; no unrelated metadata was changed.

## Deleted migration surfaces

- D3/D4 contract checkers, generators, script unit tests, JSON projections,
  schemas, hashes, and migration READMEs.
- The build-local `experimental-actors` feature and five actor migration
  linkages from the standard/full native-host catalog.
- The actor source-generator mode, runtime configuration, generated bytecode
  fixture, source fixture, corpus entry, and generator feature selection.
- Migration-era CI commands for the deleted D3/D4 projections and the deleted
  root `test` product feature.

The two substantive resident-external source fixtures moved byte-for-byte to
`tests/fixtures/resident-external/`. The permanent runtime test now reads those
paths. The D3 Gate D report and benchmark runner remain unchanged.

## Permanent owners retained

- Permanent architecture qualification still runs value-system, artifact,
  operation, activation, D1, D2, bytecode, distribution, routing, and Gate D
  contracts. Architecture-owned changes automatically request the full
  validation workflow.
- `scripts/check-production-resident-routing.py` prevents deleted D3/D4
  projection scaffolding from returning.
- `scripts/check-gate-d-contract.py` retains the authoritative D3 evidence pin
  without depending on the deleted phase projection directory.
- Standard/full native catalogs retain CLI, console, scene, time, timer, and
  robot arm and permanently assert zero actor migration functions.
- The generic actor bootstrap data model, actor/task/store runtime structures,
  and production `NativeActorBootstrapUnsupported` rejection remain intact.
  Permanent actor-planning tests use an explicit synthetic test catalog rather
  than borrowing shipping catalog entries.
- Core bytecode tests retain synthetic `ApplicationRequirement::HostFunction`
  writer, reader, canonical-order, reference, instruction-flow, and rejection
  coverage. The bytecode-v1 corpus remains format-compatible and deterministic
  with 20 fixtures instead of 21.
- Current inventory, migration classifications, frozen semantic targets, and
  immutable-growth guards remain installed.

## Validation record

The following E3-owned proofs pass at the closeout tree:

- repository patch hygiene and formatting;
- runtime wildcard-import, unsafe-boundary, and complete runtime-boundary
  audits;
- complete function-system migration qualification, including the 9,010
  runtime-factory surface;
- bytecode compiler/runtime boundary qualification, including all 16
  bytecode-topology tests and the isolated producer/consumer round trip;
- permanent production-resident routing and Gate D evidence checks;
- bytecode-v1 static format contract with 20 fixtures;
- native-host catalog contract with five standard providers, six full
  providers, and no actor migration linkage;
- 117 `mech-build` all-feature unit tests, including permanent actor-bootstrap
  analysis and fail-closed rendering tests;
- the focused full-host native-pruning test, with the full planning catalog
  pruned deterministically to the console-only artifact owner;
- 82 focused core bytecode tests, including generic host-function codec
  coverage;
- current value-system inventory generation;
- the exhaustive value-system contract with exactly
  `[C0-GATE-B-EVIDENCE-STALE]` and no occurrence, classification,
  frozen-target, matrix-adapter, inventory, or immutable-growth failure;
- no diff from the exact E2 base under `benchmarks/runtime/`.

The GitHub impact job automatically requested the full validation workflow on
the pushed E3 head; full validation was not skipped. The first complete run,
`31821030259`, finished with 53 successful jobs and exposed the permanent
qualification gaps recorded below. E3-owned checker and generated-build
failures found by that run were corrected and locally replayed. Commands that
remain blocked are recorded below rather than described as passing.

## Mandatory-stop record

### Unsupported bare root `run` feature canary

- Attempted target: replace deleted `test`-feature CI coverage with a bare root
  `run` feature canary.
- Command: `cargo +nightly-2026-03-03 check --locked --no-default-features
  --features run`.
- Error: the root `run` feature does not close the source catalog and resident
  routing surface needed by the current CLI implementation.
- E2 reproduction: yes, on exact base
  `f16e6b5d8a01b226353aba914c7c8ac1fcc90e31`.
- Permanent owner: the supported `distribution-standard` product profile.
- Disposition: the workflow uses supported `distribution-standard` run/build
  canaries. No root feature graph or runtime semantics changed.

### Resident-external integration feature closure

- Attempted target: run the preserved Gate D resident-external integration
  test after moving its source fixtures.
- Command: `cargo +nightly-2026-03-03 test --locked -p mech-runtime --test
  resident_external_gate_d3 --no-default-features --features
  runtime_bench_gate_d3`.
- Error: `runtime_bench_gate_d3` selects `resident-external` without the
  `resident-routing` closure (`ResidentRouteFailureClass`, `route_failure`, and
  active-program symbols are unavailable), followed by the unchanged
  non-exhaustive `ResidentExecutionError::InvalidOutputMaterialization` match
  in resident recording.
- The prescribed `python3 scripts/generate-d2-contract.py --check` reaches the
  same non-exhaustive resident-recording match after D1 generation succeeds;
  it is the same feature-profile closure stop, not a D2 projection mismatch.
- E2 reproduction: yes, on the exact E2 base.
- Permanent owner: the preserved resident-external/Gate D runtime integration
  test.
- Disposition: moved fixtures and their test references remain; runtime code,
  feature boundaries, and benchmark evidence are unchanged. The focused
  source/bytecode artifact-equivalence owner passes. Final feature-closure
  resolution remains part of exhaustive F0 validation.

### Bytecode corpus source-generator base failures

- Attempted target: regenerate/check the 20-fixture bytecode corpus after
  deleting the actor application.
- Commands:
  `cargo +nightly-2026-03-03 run --locked --manifest-path
  tests/fixtures/bytecode-v1-generator/Cargo.toml -- --write` and the prescribed
  `--check` form.
- Error: untouched E2 `--check` first fails actor source compilation with
  `MissingFunction` for `actor/state/id`. Once E3 removes that obsolete
  fixture, generation reaches the synthetic-live source and fails because
  module manifest `test-live` is reported unregistered.
- E2 reproduction: yes. The exact E2 source-generator executable reproduces
  the synthetic-live failure when invoked directly with the retained source.
- Permanent owner: the bytecode-v1 static corpus contract, core bytecode codec
  tests, and retained synthetic-live source compiler fixture.
- Disposition: removed only the actor file/entry and mechanically updated the
  deterministic manifest/hash expectations. Every remaining committed fixture
  byte is unchanged, the static corpus contract and core codec tests pass, and
  compiler/runtime semantics are untouched. The generator command remains an
  explicit F0 root-bytecode stop rather than a false E3 pass.

The full-CI run also showed that the bytecode determinism job had fetched only
the outer generator graph before invoking the inner source generator offline.
E3 now fetches both locked graphs. The surviving native-live generator was
also corrected to count only `resident_turn`; its deleted `outcome.turn` field
no longer appears in generated source. The generated executable then reaches
the retained `synthetic-live-read.mecb` and rejects it as an obsolete artifact
shape (`invalid type: map, expected a sequence`). That fixture/runtime mismatch
remains part of this bytecode stop and was not hidden by disabling the test.

### Generated fixed-matrix native project closure

- Attempted target: complete `cargo +nightly-2026-03-03 test --locked -p
  mech-build --all-features` after all 117 build unit tests passed.
- Failing owner: `native_generated_end_to_end` while building
  `generated_native_fixed_matrix`.
- Error: the isolated generated project lacks broad `bool`/`Vector2` engine
  type and shape features referenced by the assignment factory macros (152
  `FunctionRuntimeType`, `MatrixBool`, and `Vector2` errors).
- E2 reproduction: the fixed owner profile and every failing engine/build
  production source are byte-for-byte unchanged from E2; the removed
  `experimental-actors` forwarding belonged only to the separate standard
  profile. An additional full E2 rebuild was not used as evidence after it
  consumed 74 GB; exact execution remains an F0 generated-native check.
- Permanent owner: fixed-matrix generated-native application closure.
- Disposition: the failing product test remains enabled. E3 does not change
  engine factory semantics, broaden generated feature closures, or weaken the
  test.

### Frozen distribution and feature-profile disagreements

- Full CI currently requests `standard-runtime`, `standard-source`, and
  `standard-compiler` from `check-static-distribution-profiles.sh`, but that
  permanent checker owns `selected-runtime` and `full-*` profiles. Its size
  matrix likewise requests three unsupported `standard-*` names, and native
  linkage requests a `standard` surface where the checker owns `full` plus
  extended shards.
- Automatic review rejected replacing those names with `full-*`, because that
  would silently remove intended standard coverage. The workflow matrices are
  therefore left visible and failing until real standard profile owners are
  implemented or the required coverage is explicitly redefined.
- With dev-only Cargo edges correctly excluded, the shipping WASM
  `browser_project` graph still enables `mech-combinatorics`, while the frozen
  WASM-source profile explicitly forbids it. This is a real profile-contract
  disagreement, not dev-dependency contamination.
- The supported selected-runtime and full-runtime static profiles pass. The
  full-source catalog constructs 9,011 runtime factories while its frozen
  assertion expects 9,010. E3 does not refresh that permanent surface without
  identifying the additional factory. Full-compiler was not described as
  passing after the serial validation stopped at full-source.
- Bare `run`, `serve`, `browser_project_runner`, and core `no_std` qualification
  jobs also lack parts of the current resident/source closure. E3 keeps those
  checks visible instead of replacing them with broader profiles after
  automatic review classified that replacement as a coverage reduction.

### Retained product qualification gaps

- The native analog-clock smoke path reaches resident activation and fails at
  `core/composite-pack` with `UnsupportedConstruction` at node 14. This is a
  retained product test and remains enabled.
- The WASM bytecode suite passes 15 of 16 browser tests. The remaining inline
  document test expects resident rejection, while the current product admits
  the document. E3 does not choose the product semantic by rewriting either
  the runtime or the assertion.
- The native hosted build suite still asserts that two source roots compile in
  caller order, while production resident builds reject multiple roots with
  `MultipleRootsUnsupported`. Automatic review rejected changing that product
  assertion during E3, so the disagreement remains explicit.
- The runtime boundary job previously stopped on wildcard imports in inline
  test modules and `tests.rs`. E3 now scopes the production audit correctly and
  removes the remaining inline-test wildcards; the complete boundary audit
  passes locally.

### Windows distribution packaging

- Both standard and full Windows release-package jobs build their optimized
  executables, then fail inside `package-distribution.py`. The wrapper currently
  suppresses the child packager's diagnostic, so the exact Windows exception
  is not preserved in the Actions log.
- The likely cross-volume temporary-file replacement needs a focused Windows
  reproduction, but automatic review rejected changing shipping packaging in
  E3 without that proof. Linux/macOS packaging owners and the local packaging
  contract remain unchanged; Windows packaging remains an explicit F0 stop.

### Automatic-review stop dispositions

- Rejected: rewriting the multi-root build test from success to rejection.
  Disposition: retained product assertion preserved and gap logged.
- Rejected: changing the Windows packager's temporary-directory placement.
  Disposition: shipping packager unchanged pending a Windows reproduction.
- Rejected: replacing standard distribution/linkage matrices with full
  profiles or removing `no_std`, WASM subprofile, run-only, and live-resource
  coverage. Disposition: disputed coverage remains visible. Only the deleted
  `native_live_resource` target was replaced with the surviving generated-live
  end-to-end owner.

No stop restored interpreter execution, added a host semantic contract,
deleted a retained host, weakened a current product assertion, refreshed a
runtime-factory surface, or refreshed a benchmark number.

## Unchanged product boundaries

- Resident execution and publication/effect sequencing are unchanged.
- CLI, browser DOM, console, scene, time, timer, and robot-arm behavior is
  unchanged.
- Bytecode v1 wire format and codec production code are unchanged.
- Browser delegation compatibility bytes are unchanged.
- Gate B and Gate D benchmark reports and all benchmark measurements are
  unchanged.
- Shipping interpreter/fallback reachability remains absent.

## Exact F0 handoff

F0 contains only:

1. controlled Gate B/D evidence refresh against the exact final stack;
2. resolution of the two EKF fault-injection issues;
3. resolution of root bytecode/source-generator, generated-native, retained
   product, feature-profile, and Windows packaging failures exposed by the
   exact E3 full-CI run;
4. exhaustive standard/full source, bytecode, generated native, WASM, browser,
   CLI, host, capability, replay, effect, packaging, and distribution
   validation;
5. final proof of zero shipping interpreter/fallback reachability; and
6. reproducible browser, driver, and toolchain pinning.

E3 creates no additional product or host behavior. It hands the explicitly
logged permanent qualification gaps to F0 without misclassifying them as
migration cleanup or silently weakening their owners.
