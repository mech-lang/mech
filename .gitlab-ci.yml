variables:
  GIT_SUBMODULE_STRATEGY: recursive

image: "rust:latest"

test:cargo:
  script:
  - rustup default nightly-2021-12-25
  - rustup show
  - rustc --version && cargo --version
  - rustup component add rust-src
  - cargo build --bin mech --release
  - ./target/release/mech test tests