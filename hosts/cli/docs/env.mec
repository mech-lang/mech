CLI Environment Context
===============================================================================

```mech:disabled
+> @env := cli/env

home := @env/HOME
```

Environment reads require the CLI environment capability profile and may be narrowed by config.
